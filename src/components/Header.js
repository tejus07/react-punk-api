// Header.js
import React from 'react';

const Header = () => {
    const headerStyles = {
        padding: '20px 20px 0px 20px',
    };

    return (
        <header style={headerStyles}>
            <h1>React Project made with PunkAPI</h1>
        </header>
    );
};

export default Header;
